# Telegram Bot Token (required)
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Internal Secret for step chain (generate: openssl rand -hex 32)
COUNCIL_SECRET=

# Mistral API key (users set their own via /setkey — not here)
